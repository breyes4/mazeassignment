package edu.wctc;

public interface Exit {
    //Contains method named "exit" that takes a Player object as an argument and returns a String.
    // String character is going to be displayed to the Player after they exit the Room.
    public String exit(Player player);
}